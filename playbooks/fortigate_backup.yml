---
- name: Backup current config
  tasks:
    - name: backup
      fortios_config:
        host: 102.168.0.99
        username: admin
        password: Start-123
        backup: yes
        backup_path: /etc/ansible/
